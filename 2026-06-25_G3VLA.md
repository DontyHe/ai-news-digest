# G³VLA: Geometric Inductive Bias for Vision-Language-Action Models

> 📅 2026-06-23 | 🔗 [arXiv:2606.24472](https://arxiv.org/abs/2606.24472) | 🏛️ NYU Shanghai, DTU, MBZUAI, NYU Abu Dhabi | 投稿CoRL

## 一句话概括
通过相机感知的几何模块(ray embedding + PRoPE + cross-view fusion)向预训练VLA注入标定几何先验，在不修改backbone和action空间的前提下提升空间泛化能力。

## 论文信息
- **作者**: Yue Peng, Yongzhe Zhao, Artur Habuda, Khuyen Pham, Yanheng Zhu, Tran Nguyen Le, Fares Abu-Dakka, Li Guo
- **机构**: NYU Shanghai, Technical University of Denmark, MBZUAI, NYU Abu Dhabi
- **方向**: VLA, 几何归纳偏置, 多相机感知
- **状态**: Submitted to CoRL 2026
- **项目页**: [https://sites.google.com/view/g3vla](https://sites.google.com/view/g3vla)

## 摘要
VLA模型利用预训练视觉语言backbone的语义知识在通用机器人操纵上取得进展，但其视觉token仍锚定于2D图像坐标，而非机器人相机的标定几何。这在多相机设置中尤为突出：视角通过已知的内外参耦合，却被当作独立图像处理。G³VLA提出相机感知的几何模块，通过内参条件化光线嵌入(intrinsic-conditioned ray embeddings)、投影位置编码(PRoPE)和双向跨视角融合(bidirectional cross-view fusion)，向预训练VLA的视觉token流中注入标定结构，不改变action空间或模仿学习目标。几何监督来自ground-truth点图(仿真中可用)或置信度门控的π³X teacher预测(无需深度传感器)。在π₀上，G³VLA在LIBERO、RoboCasa24、RoboTwin2.0和真实机器人上均取得一致提升，在空间敏感和物体敏感任务上提升最大。进一步在π₀.₅和GR00T 1.5上验证。

## 架构分析

### 三个几何模块
1. **Intrinsic-conditioned Ray Embeddings**: 利用相机内参矩阵K的逆K⁻¹，将每个ViT patch token标记其反向投影的视线方向。每个token获得一个3D viewing direction embedding，编码该patch在3D空间中的位置信息
2. **Projective Positional Encoding (PRoPE)**: 在标准rotary positional embedding基础上增加相机标定的偏置编码，编码跨视角的投影关系。不同视角的token可以通过投影关系相互对齐
3. **Bidirectional Cross-view Fusion**: 在token到达action model之前，双向交换几何上下文。所有有效token跨视角双向attend，以PRoPE作为位置信号

### 两阶段训练
- **Stage 1: 几何模块预训练**: 冻结backbone，仅更新ray embedding、cross-view fusion和辅助point head。使用π³X作为离线几何teacher，获取逐像素目标和置信度logits，通过置信度门控(τ=0.1)过滤低质量预测
- **Stage 2: 全策略微调**: 从Stage 1 checkpoint开始，解冻所有参数，action loss主导，保留distillation作为轻量几何正则化

### 辅助Point Head
在融合视觉token上附加轻量transformer + 卷积上采样器，预测逐像素ray坐标q̂(ℝ²)和log-z深度d̂。推理时丢弃。

## 实验结果

### 仿真结果 (π₀ backbone)
| Benchmark | 提升 |
|-----------|------|
| LIBERO suites | 一致提升 |
| RoboCasa24 | 一致提升 |
| RoboTwin2.0 | 一致提升 |

在空间敏感(涉及精确位姿估计)和物体敏感(需要区分相似物体)任务上提升最大。

### 跨架构验证
| Backbone | 结果 |
|----------|------|
| π₀ | 一致提升 |
| π₀.₅ | 进一步提升(已接近饱和的LIBERO) |
| GR00T 1.5 | 效果混合 |

**关键发现**: GR00T 1.5的双塔架构(diffusion policy通过cross-attention访问frozen VLM)可能削弱几何信号。这暗示几何注入的收益取决于geometry-aware token参与action生成的直接程度。

## 创新点
1. **零侵入式几何注入**: 不修改backbone、不改变action space、不改变模仿学习目标，仅在视觉token层面注入几何结构
2. **π³X几何蒸馏**: 无需深度传感器或人工标注，利用预训练视觉几何teacher的置信度门控预测提供监督
3. **多架构验证**: 首次在π₀、π₀.₅、GR00T 1.5三个不同架构上验证几何归纳偏置的迁移性
4. **架构洞察**: 发现几何注入效果与geometry-aware token到action pathway的直接程度相关

## 对具身智能的启发
- **VLA的几何缺陷**: 2D token表示是VLA空间能力的瓶颈，多相机系统中已知的内外参几何结构被浪费
- **轻量级几何归纳偏置**: 可以在不牺牲VLM语义能力的前提下，通过token-level注入提升空间精度
- **架构设计启示**: VLA的action pathway如何消费视觉token决定了能否有效利用几何信息，frozen VLM + cross-attention的架构可能不适合
- **实用价值**: 仅需RGB + 相机标定参数，推理时不依赖深度传感器或额外teacher
- **局限**: 几何模块需要额外的预训练阶段；在GR00T上的混合结果需要更深入分析

## 相关论文
- SpatialVLA, 3D-VLA: 需要显式3D传感器输入或大规模空间预训练的替代方案
- PerAct, Act3D, RVT: 显式3D表示的操纵方法
- π³X: 视觉几何teacher，提供dense point map预测
