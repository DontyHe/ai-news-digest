# arXiv 具身智能/VLA/世界模型 论文周报

> 日期范围：2026-07-29 至 2026-08-05
> 自动生成时间：2026-08-05

---

## 1. WCM: World Critic Model（世界评论家模型）

**一句话概括**: 将世界建模与价值估计统一到一个轻量级架构中，解决 VLA 强化学习中 critic 的部分可观测性问题。

**论文信息**
- **标题**: WCM: A World Critic Model for Vision-Language-Action Reinforcement Learning
- **作者**: Senyu Fei, Xiaopeng Yu, Siyin Wang, Xianzhong Zhao, Jingjing Gong, Xipeng Qiu
- **机构**: 同济大学、上海创新学院、复旦大学
- **arXiv ID**: [2607.29613](https://arxiv.org/abs/2607.29613)
- **发表日期**: 2026-07-31

**摘要**

VLA 模型的 RL 后训练中，critic 主要依赖单帧观测来估计价值函数。这在机器人操控的 POMDP 场景下是根本性的不匹配：单帧无法捕获运动、接触进度等关键动态信息。本文提出 World Critic Model (WCM)，基于轻量级 LeJEPA 架构，将未来潜在状态预测与价值估计联合训练，使 critic 的表示被显式训练来捕获时序动态，而非仅仅回归标量回报。在 4 个 benchmark 的 149 个任务上，WCM 一致取得 SOTA。

**架构分析**

WCM 的核心洞察是：在部分可观测场景下，单纯给 critic 增加历史帧输入是不够的，因为标量价值回归提供的监督信号太弱，无法学习跨时间步的动态。需要的是让 critic 的表示空间能预测未来状态。

架构包含四个组件：观测编码器、世界预测器、价值解码头和世界解码头。观测编码器将历史 K 帧分别编码为潜在嵌入 z_t-k = enc(o_t-k)。语言指令通过 CLIP 编码后经 adapter 映射到 WCM 潜在空间。编码后的视觉历史先与指令 token 做交叉注意力，然后通过因果 Transformer 历史 trunk（world predictor）处理。

隐藏表示 h_t 被送入两个解码头：价值头 V_t = D_value(h_t) 估计回报，世界预测头通过残差更新预测下一帧潜在状态 z_{t+1} = D_world(h_t, a_t, z_t)，其中 D_world 使用 action encoder 和 gated FiLM 残差块。

训练目标包含三部分：预测损失 L_pred（teacher-forcing 下预测潜在状态与真实状态的 L2 距离）、SIGReg 正则化（防止特征坍缩到各向同性高斯）、以及价值估计损失。

**实验结果**

在 4 个 benchmark（LIBERO-Spatial/LIBERO-Object/LIBERO-Long/RLBench）的 149 个任务上，WCM 与 π0、π0.5、OpenVLA-OFT 等骨干网络兼容，在 in-distribution 和 out-of-distribution 设置下一致超越现有方法。在 7 个真实世界操控任务上使用 OpenVLA-OFT 和 π0.5 进行 off-policy RL，WCM 同样超越标准 VLM-critic baseline。消融实验验证了世界预测目标的必要性：去掉预测损失后性能显著下降。

**创新点**
- 首次将世界建模目标引入 VLA-RL 的 critic 设计，解决部分可观测性下的状态近似问题
- 基于 LeJEPA 的轻量级统一架构，同时预测未来潜在状态和估计价值
- 兼容 on-policy（PPO/Flow-SDE）和 off-policy（AWR/RECAP）训练管线
- 无需修改 VLA 骨干网络，作为即插即用模块使用

**对具身智能算法工程师的启发**
- 在做 VLA 的 RL 后训练时，不要忽视 critic 的设计。单帧 critic 是性能瓶颈之一
- 世界建模的辅助损失可以作为 critic 的正则化信号，提升泛化能力
- LeJEPA 架构适合作为 critic 的基础，因为它天然支持潜在状态预测

---

## 2. Faster-WAM: World Action Models 不需要深度 Action 模块

**一句话概括**: 通过 Dock of Transformer 机制，用单层 action DiT 搭配 30 层视频骨干，将 WAM 推理速度提升 3.2 倍。

**论文信息**
- **标题**: Faster-WAM: Do World Action Models Need Deep Action Modules?
- **作者**: Liheng Ma, Rui Heng Yang, Zhanguang Zhang, Mateo Clemente, Ziwen Hu, Tongtong Cao, Yingxue Zhang
- **机构**: 华为诺亚方舟实验室
- **arXiv ID**: [2608.02365](https://arxiv.org/abs/2608.02365)
- **发表日期**: 2026-08-03

**摘要**

现有 WAM 采用 shared-backbone 或 Mixture-of-Transformers (MoT) 设计，action 模块的深度与视频骨干绑定，导致推理延迟高。本文提出 Dock of Transformer (DoT)，将预训练视频 Transformer 视为表示中枢，通过 docking interface 连接轻量输出头。Faster-WAM 将单层 action head 对接到 30 层视频骨干上，在无需额外具身预训练的情况下达到竞争性性能。

**架构分析**

DoT 的核心设计原则是 video-centric：视频世界模型作为表示中枢，任务专用头通过 docking interface 访问所有层的表示。这打破了 MoT 架构中 video-action 层之间严格的 1:1 对应关系。

Faster-WAM 的 docking mechanism 包含两个组件：

1. **KV-Fusion**: 对视频骨干所有层的 key 和 value 做跨层聚合。先通过 channel mixing（可学习投影 W^K, W^V）将视频 KV 映射到 action head 的特征空间，再通过 layer mixing（可学习聚合矩阵 A_h）沿层维度聚合，形成融合的 KV cache。

2. **Video-Action RoPE Alignment**: 视频使用 3D-RoPE（编码时间、空间高度、空间宽度），action 使用 1D-RoPE（编码动作序列位置）。论文提出对齐机制解决坐标系统不匹配问题。

单层 action head 的混合上下文注意力将自己 action KV cache 与 docked video KV cache 拼接后计算。

**实验结果**

LIBERO 上 98.50%，RoboTwin 2.0 上 89.17%，LIBERO-Plus 上 75.0%（比 Fast-WAM 高 23.5 个百分点）。推理延迟 66.5ms per inference，比 Fast-WAM（211.7ms）快 3.2 倍，也低于 π0（116.1ms）和 π0.5（71.4ms）。

**创新点**
- 提出 DoT 框架，打破 MoT 的 1:1 层对应限制，允许任意深度的输出头
- KV-Fusion 实现跨层 KV 聚合，channel mixing + layer mixing 两步设计
- Video-Action RoPE alignment 解决 3D/1D RoPE 坐标系不匹配
- 在保持性能的同时大幅降低推理延迟

**对具身智能算法工程师的启发**
- WAM 的 action 模块不需要和视频骨干一样深，单层 DiT 足够
- 如果你在设计 WAM 架构，考虑将视频骨干作为特征提取器而非端到端训练
- 推理延迟是 WAM 落地的关键瓶颈，DoT 提供了有效的解耦思路

---

## 3. DreamTrajectory: 轨迹引导的移动操控世界模型对齐

**一句话概括**: 联合生成末端轨迹和全身动作块，并通过轨迹世界模型做 test-time refinement，解决移动操控中的协调性和一致性难题。

**论文信息**
- **标题**: DreamTrajectory: Trajectory-Guided Action Generation with World Model Alignment for Mobile Manipulation
- **作者**: Zheng Yang, Wenjie Zhang, Xiangyu Chen, Wenxuan Song, Xianpeng Wang, Yihang Kang, Wen Chen, Lujia Wang, Renjing Xu, Xiaowen Chu
- **机构**: 香港中文大学（深圳）
- **arXiv ID**: [2608.01381](https://arxiv.org/abs/2608.01381)
- **发表日期**: 2026-08-02

**摘要**

移动操控需要协调底盘和机械臂运动。现有 VLA 存在两个问题：直接映射到全身动作块缺少显式运动规划；开环执行无法验证动作是否符合预期。DreamTrajectory 在单个 action expert 中联合预测意图级末端轨迹和全身动作块，轨迹作为 task-space 引导。test-time 阶段通过轻量级轨迹世界模型做 search-predict-score refinement。

**架构分析**

DreamTrajectory 的核心假设：末端轨迹是全身控制的有意义的中间表示，它指定夹爪该往哪走，再由底盘和机械臂决定怎么走。

轨迹表示为未来末端执行器的 7D pose 序列 τ_{t:t+H-1}，每个 waypoint 包含在 chunk-local frame（固定于当前底盘 pose）下的位置和四元数。轨迹和动作块共享 horizon H，由双流 action expert 联合生成：

(τ̂, â) ~ π_θ(τ, a | o_t, s_t, ℓ)

采用 conditional flow matching，两个流共享 flow time σ 使得去噪过程同步演化。关键设计：group-causal attention mask，轨迹流可以条件化动作流，但动作流不能影响轨迹流。

轨迹世界模型（约 49M 参数）预测候选动作块将诱导的末端轨迹。test-time 做 search-predict-score：采样 N 个动作候选，用世界模型预测各自的诱导轨迹，选择与规划轨迹最一致且平滑的候选。因为评估在紧凑的轨迹空间而非像素空间进行，所以非常高效。

**实验结果**

MS-HAB 的 6 个 set_table 子任务：baseline π0.5 平均成功率 32.3%，加入轨迹引导后 47.5%，test-time refinement 后 54.8%。contact-rich 的铰接物体任务（如开冰箱、拉抽屉）提升最大。3 个真实世界任务：63.3% → 81.7% → 90.0%。

**创新点**
- 首次在 VLA 中联合生成末端轨迹和全身动作块，group-causal attention 实现单向引导
- 轻量级轨迹世界模型（49M 参数）做 test-time refinement，无需重新训练策略
- 在轨迹空间而非像素空间评估候选动作，效率极高
- 专注于移动操控场景，解决底盘-机械臂协调难题

**对具身智能算法工程师的启发**
- 移动操控中，显式的轨迹中间表示比直接 end-to-end 有效得多
- Test-time refinement 在轨迹空间做比在像素空间做更高效，值得借鉴
- Group-causal attention 是实现多流生成的优雅方案

---

## 4. World Action Planner: 基于动作条件世界模型的泛化决策

**一句话概括**: 利用 VLM 推理能力 + 多任务姿态-图像条件世界模型，通过 world model 想象进行 iterative action plan refinement。

**论文信息**
- **标题**: World Action Planner: Generalizable Decision-Making with Action-Conditioned World Models
- **作者**: Xiangcheng Zhang, Yilun Du
- **机构**: 哈佛大学
- **arXiv ID**: [2607.27599](https://arxiv.org/abs/2607.27599)
- **发表日期**: 2026-07-29

**摘要**

端到端模仿学习策略在新场景和任务上泛化能力有限。本文提出 World Action Planner，利用 VLM 的推理能力和多任务 pose-image 条件世界模型的物理基础，使 agent 能提出初始动作计划并通过世界模型想象进行迭代优化。在组合任务、新布局和零样本泛化场景上显著超越 VLA 和 WAM 等 E2E 模型。

**架构分析**

系统包含三大组件：

1. **Pose-Image 条件世界模型**: 一种多视角世界模型，能够根据机器人姿态和当前图像精确模拟未来状态。支持对未见过的动作、场景和跨机器人泛化。

2. **VLM Agent 的动作提案**: 利用 VLM 的世界知识生成原始动作序列（MOVE, ROTATE, GRASP, RELEASE 等），通过多视角图像三角化确定 3D 目标位姿。

3. **两层优化流程**:
   - 全局优化：VLM 基于世界模型想象的 rollout 评估动作安全性，提供高层语义反馈（如"碰撞风险，增加高度"）
   - 局部搜索：在精细操控任务中，通过 GridSearch 生成动作候选，用世界模型想象各候选的执行结果，VLM 对结果排序选择最优

论文还提供理论分析，证明在 tabular 和线性函数近似下，基于模型的规划在多任务泛化上优于模仿学习。

**实验结果**

在组合长时任务中，agent 能通过想象成功管理子任务间的过渡，而 VLA 往往在第一个子任务后停滞。在空间泛化场景中，准确识别新目标位置并完成任务，而模仿学习策略倾向于到达训练集中的坐标。在零样本场景中表现优异。消融实验验证了全局优化和局部搜索的各自贡献。

**创新点**
- 系统性整合 VLM 推理 + 世界模型想象进行机器人规划
- 全局优化（语义反馈）+ 局部搜索（精确微调）的两层优化策略
- Pose-image 条件世界模型支持跨泛化
- 提供理论保证：基于模型的规划在多任务泛化上优于模仿学习

**对具身智能算法工程师的启发**
- 端到端策略的泛化能力有限，世界模型 + VLM 规划是可行替代方案
- 粗粒度语义反馈 + 细粒度局部搜索的组合设计值得参考
- 世界模型想象可以帮助检测碰撞等物理风险，提升安全性

---

## 5. DF3: 无解码器的特征预测世界模型

**一句话概括**: 完全在冻结的 ViT 编码器空间内做未来状态预测，无需任何解码器即可完成下游任务。

**论文信息**
- **标题**: DF3: World Modeling via Decoder-Free Feature Forecasting in Autonomous Navigation
- **作者**: Jiaming Chen, Guoan Xu, Aoshen Huang, Haozhuo Zhang, Yang Li, Wei Pan
- **机构**: 曼彻斯特大学、悉尼科技大学、山东大学、上海交通大学
- **arXiv ID**: [2608.02428](https://arxiv.org/abs/2608.02428)
- **发表日期**: 2026-08-03

**摘要**

现有世界模型方法在像素级或潜在特征级预测未来，但都依赖重量级解码器。本文提出 DF3（Decoder-Free Feature Forecasting），完全在冻结的视觉基础模型（DINO ViT）的编码器空间内进行未来状态预测和下游任务输出。通过可学习的空间 query 注入 ViT 终端层，配合 Motion-Aware Context Fusion 机制，实现高效的特征预测。

**架构分析**

DF3 的核心思想：既然 DINO ViT 的中间层特征已经足够丰富，为什么还要解码后再重新编码？

架构设计：给定历史帧序列 {I_1, ..., I_T}，用冻结的 DINO ViT 的早期层 L1 提取每帧的 context features（包含 prefix tokens 和 patch tokens）。引入可学习的 prediction queries Q_pred 注入到 ViT 的终端层 L2，与历史 context features 交互后，经 MACF 机制精炼，输出下一帧的预测特征。

**Motion-Aware Context Fusion (MACF)**: 冻结的 ViT 本身缺乏时序建模能力。MACF 统一了粗粒度光流 warp 和细粒度潜在交叉相关，显式建模帧间运动，使 query 能准确预测未来 token 表示。包含 flow alignment stage（粗粒度对齐）和 cross-correlation stage（细粒度交互）。

预测出的下一帧特征 F̂(I_{T+1}) 再注入另一组 task queries，直接在 ViT 终端层提取任务特定表示（如语义分割），完全不需要解码器。

**实验结果**

Cityscapes benchmark 上与 SOTA 潜在方法性能相当，但推理延迟和 GPU 峰值内存显著更低。零样本部署到四足机器人仿真器中进行自主导航，展示了实际应用价值。

**创新点**
- 首个完全在冻结 VFM 编码器空间内做未来预测的框架，消除解码器瓶颈
- MACF 机制融合光流 warp 和潜在交叉相关，无需微调 ViT
- Prediction queries 和 task queries 可独立训练，灵活性高
- 真正的 decoder-free 设计，适合延迟敏感的机器人应用

**对具身智能算法工程师的启发**
- 世界模型不一定需要像素级重建或大解码器，在编码器空间做预测更高效
- 冻结 VFM + 可学习 query 注入是一种有效的适配策略
- 对于导航等延迟敏感任务，decoder-free 方案有实际部署价值

---

## 6. ODEWorld: 基于物理时间流的连续预测架构

**一句话概括**: 用 ODE 在物理时间上建模潜在速度场，实现连续时间世界模型，支持任意时间分辨率和反向预测。

**论文信息**
- **标题**: ODEWorld: A Continuous Predictive Architecture via Physical-Time Flow
- **作者**: Dongxiu Liu, Haoyi Niu, Peng Cheng, Yuan Gao, Xirui Kang, Sangli Teng, Koushil Sreenath, Xianyuan Zhan
- **机构**: 清华大学 AIR、UC Berkeley BAIR
- **arXiv ID**: [2607.27924](https://arxiv.org/abs/2607.27924)
- **发表日期**: 2026-07-30

**摘要**

物理世界的时空本质是连续的，但现有世界模型采用离散时间预测。本文提出 Physical-Time Flow (PT-Flow)，在物理时间上学习连续潜在速度场，潜在状态转移由 ODE 控制。构建 ODEWorld，通过动力学表示解耦和直接一阶监督，解决了 JEPA 框架中的表示坍缩问题。支持任意时间分辨率、反向预测和长时域视频生成，同时为下游策略学习提供丰富的规划信息。

**架构分析**

PT-Flow 的两个关键设计：

1. **动力学表示解耦**: 学习初始状态条件化的编码器 f_dyn(s_t; s_0) = z_t 和解码器 g_dyn(z_t; s_0) = s_t。将静态上下文（背景、时间不变物体）从编码/解码中分离出来，使潜在空间 z_t 专注于动力学变化。重建损失 L_dyn-recon = ||g_dyn(f_dyn(s_t; s_0); s_0) - s_t||²。

2. **直接一阶监督**: 潜在速度 ż_t = ∂f_dyn/∂s_t · ṡ_t，可以通过 Jacobian-vector product (JVP) 从离散时间数据中近似获得真实潜在速度的监督信号，直接训练速度场 v_θ。

ODE 形式: dz_t/dt = v_θ(z_t, t; z_0, c)，预测未来就是 z_T = z_0 + ∫₀ᵀ v_θ(z_t, t) dt。

ODEWorld 的优势：连续性允许任意时间分辨率、反向预测；ODE 性质 + 直接速度监督有效防止表示坍缩；学习到的潜在速度场产生可解释的平滑变换路径。

**实验结果**

在 LIBERO 和 AgiBot-World 数据集上验证。视频生成质量与 SOTA 相当，且支持长时域重建而不坍缩。策略学习实验表明 ODEWorld 提取的表示能有效促进下游策略学习。支持从不规则采样或丢帧数据中生成视频。

**创新点**
- 首个基于物理时间 ODE 的连续时间潜在世界模型
- 动力学表示解耦 + 直接一阶监督，从设计上解决 JEPA 的表示坍缩问题
- 支持任意时间分辨率、反向预测、不规则采样
- 在视频生成和机器人控制两个任务上都有效

**对具身智能算法工程师的启发**
- 连续时间建模可能是世界模型的一个有前景方向，特别适合需要精细时间控制的任务
- 表示解耦思路（分离静态和动态信息）可以应用到其他世界模型设计
- ODE 的数学性质（可逆性、连续性）为规划提供了额外工具

---

## 7. EgoGenesis: 自我中心世界动作建模与在线锚定投射记忆

**一句话概括**: 通过 OAPM 保持 3D 场景锚定、A3D-RoPE 注入动作几何，生成高质量自我中心操控视频用于数据增强。

**论文信息**
- **标题**: EgoGenesis: Egocentric World-Action Modeling with Online Anchored Projective Memory and Action-3D RoPE
- **作者**: Zexuan Yan, Yuzhou Wu, Yue Ma, Zonghang He, Kaibo Yin, Xiaobing Tu, Yinggui Wang, Jinkui Ren, Xiantao Zhang, Shijian Wang, Jinghong Liu, Linfeng Zhang
- **机构**: 上海交通大学、阿里巴巴集团、HKUST、东南大学、人民大学、东京大学
- **arXiv ID**: [2607.28243](https://arxiv.org/abs/2607.28243)
- **发表日期**: 2026-07-30

**摘要**

训练 WAM 需要大量自我中心视角数据，但真实数据采集昂贵。本文提出 EgoGenesis，基于预训练视频生成先验，通过两个几何感知条件机制生成可控的自我中心操控视频。在线锚定投射记忆 (OAPM) 保持首帧 3D 场景锚定并周期性刷新近期状态。动作-3D RoPE (A3D-RoPE) 将末端执行器运动编码为相机感知的 3D 旋转坐标。用 400 条生成轨迹增强 400 条真实轨迹后，单臂 OOD 成功率从 77% 提升到 84%，双臂从 53% 提升到 70%。

**架构分析**

两大核心组件：

1. **OAPM (Online Anchored Projective Memory)**:
   - 维护两个 3D 场景 slot：不可变的首帧锚定 M_a 和可刷新的近期快照 M_r
   - 通过预训练 VGGT-Ω 提取 3D 特征
   - 每隔 s_r 步解码已生成的视频前缀，编码最新 RGB 帧替换 M_r
   - 锚定永不覆写，保证场景一致性；近期状态通过 learned gating 更新
   - 选择的 DiT 层通过 gated cross-attention 读取两个 slot

2. **A3D-RoPE (Action-3D Rotary Position Embedding)**:
   - 将 skeleton/末端执行器关节栅格化到视频 latent patch 网格上
   - 用相机轨迹 unproject 到 3D，得到参考帧下的 metric 坐标
   - 将 3D 坐标编码为 RoPE 相位，注入 skeleton-to-video cross-attention
   - 保持预训练 WAN 的 self-attention 位置编码路径不变

训练数据覆盖人手、灵巧手、平行夹爪和机械臂末端执行器，防止过拟合到特定执行器。

**实验结果**

生成质量：PSNR 21.86、SSIM 0.851、LPIPS 0.240、关键点误差 0.050，全面超越 Wan2.1-14B、Cosmos3-Nano、EgoSim、RynnWorld-TeleOp 等基线。下游 WAM 泛化：在 4 个双臂和 4 个单臂真实机器人技能上，400 真实 + 400 生成轨迹使 OOD 成功率显著提升。

**创新点**
- OAPM：在线锚定投射 3D 场景记忆，保证长时生成的场景一致性
- A3D-RoPE：将 metric 3D 动作几何注入 cross-attention，实现精确动作对齐
- 在多种执行器上训练，避免过拟合
- 首次展示生成式自我中心数据可以实质性改善下游 WAM 真实世界泛化

**对具身智能算法工程师的启发**
- 数据增强不一定要依赖真实采集，高质量的视频-动作合成是可行路线
- 3D 场景锚定对长时视频生成的几何一致性至关重要
- 如果你的 WAM 泛化不够，考虑用生成模型扩充训练数据

---

> 本周趋势总结：WAM（World Action Model）是当前最热的研究方向，多篇论文从推理效率（Faster-WAM）、评论家设计（WCM）、轨迹引导（DreamTrajectory）、规划能力（World Action Planner）、连续时间建模（ODEWorld）、数据增强（EgoGenesis）等角度推进。另一趋势是 decoder-free 和轻量化设计（DF3、FibVLA），减少不必要的计算开销。
