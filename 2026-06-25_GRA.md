# Supervise What Survives: Geometry-Guided VLA Adaptation from Synthetic Robot Videos (GRA)

> 📅 2026-06-23 | 🔗 [arXiv:2606.24448](https://arxiv.org/abs/2606.24448) | 🏛️ National University of Singapore (Show Lab)

## 一句话概括
提出"非对称保留原则"，将合成机器人视频中的几何信息(而非伪动作)路由到VLA视觉backbone，结合真实示范训练动作头，实现sim2real的VLA适应。

## 论文信息
- **作者**: Danze Chen, Yanzhe Chen, Qiming Huang, Zhijun Cao, Chen Gao, Mike Zheng Shou
- **机构**: Show Lab, National University of Singapore
- **方向**: VLA, 合成数据利用, Sim2Real, 几何监督
- **Corresponding**: mike.zheng.shou@gmail.com

## 摘要
VLA模型需要大规模视频-动作对，但真实遥操作数据稀缺。合成机器人视频提供可扩展替代方案，但现有方法将其视为缺失动作标签的示范，从合成像素中恢复伪动作(pseudo-action)。本文提出这种做法是错误的抽象层匹配：视频仅捕获几何(空间轨迹，表示where)，真实示范捕获控制(电机命令，表示how)。人到机器人视频生成不等量保留这两类信息：可见的几何信息在生成过程中保留，但底层控制信号丢失。由此提出"非对称保留原则"(Asymmetric Preservation Principle)：存活的几何信息应仅用于监督视觉感知，控制学习留给真实示范。

GRA方法：从源人类视频中提取几何内容为未来2D末端执行器路标(waypoint)，经过pose估计→retargeting→物理仿真→标定投影计算得到，通过辅助2D head路由到VLA视觉backbone。动作头仅在真实示范上训练。微调时waypoint loss作为空间表征锚点(spatial representation anchor)，防止backbone在动作微调中丢失几何基础。在Franka真实机器人任务上，GRA在匹配数据预算下优于pseudo-action基线，缩小了与大量真实示范训练策略的差距。

## 架构分析

### 非对称保留原则 (核心洞察)
合成视频生成优化视觉合理性，不遵循物理控制律。两类信息在生成过程中的存活率不同：
- **几何信息(存活)**: 空间轨迹、物体排列、运动方向 → 可靠传递
- **控制信息(丢失)**: 力矩、速度、接触细节 → 在生成中消失

设计规则：监督应跟随存活信息，而非重建被擦除信息。几何指导感知，真实示范指导控制。

### GRA两阶段流程

#### Stage 1: Geometric Pretraining
- 从源人类视频提取future 2D end-effector waypoints
- Pipeline: pose估计 → 机器人retargeting → 物理仿真 → 标定相机投影
- 3层MLP head (h₂D)在合成帧上预测K步未来2D waypoints
- 使用LoRA (rank 32)更新视觉backbone φᵥ和h₂D
- 语言模型和动作头不参与Stage 1前向传播
- 训练5K步

#### Stage 2: Action Fine-tuning with Spatial Anchor
- **仅在真实示范上微调**，合成帧不进入此阶段
- 动作头φₐ从头初始化，在真实遥操作上学习delta actions (L1 loss)
- Stage 1的辅助h₂D继续接收并行waypoint信号，作为**空间表征锚点**
- 总loss: L = L_act + λ·L₂Dʳ (λ=0.5)
- LoRA (rank 32)更新φᵥ和语言模型，h₂D和φₐ全量更新
- 训练10K步

### 空间表征锚点的关键作用
发现动作微调会侵蚀Stage 1建立的几何感知表征(特征向action-predictive漂移)。持续的waypoint目标对抗这种漂移，是GRA有效性的核心机制。消融实验证实：去掉L₂Dʳ(保留Stage 1初始化)会显著退化性能。

## 实验结果

### 实验设置
- **硬件**: 7-DoF Franka Research 3, 第三人称RealSense D435
- **任务**: cube→pad, cup→coaster, mango→plate (tabletop pick-and-place)
- **数据**: 每任务25条真实遥操作 + 75条Wan 2.2生成视频 (480×640, 81帧)
- **评估**: 每任务30 trials

### 主结果
- GRA在匹配数据预算下优于所有pseudo-action基线
- 缩小了与仅用真实示范(但数量远超)训练策略的差距
- 3任务平均成功率显著优于inverse dynamics和retargeting基线

### 信息不对称诊断
实验证明合成视频的几何信息(空间结构)比控制信息(动作)更可靠，验证了非对称保留原则。

### 消融实验
- 去掉空间表征锚点(Stage 1初始化 + 无L₂Dʳ): 性能显著下降，证明锚点的必要性
- 各组件(waypoint head, geometric pretraining): 均有贡献

## 创新点
1. **非对称保留原则**: 首次形式化合成视频中几何vs控制信息的不等量保留问题，提出清晰的设计原则
2. **监督路由(Supervision Routing)**: 几何信息走视觉backbone，控制信息走动作头，两条路径解耦
3. **空间表征锚点**: 发现并解决了动作微调侵蚀几何表征的问题，持续waypoint loss作为锚点

## 对具身智能的启发
- **合成数据的正确使用方式**: 不要试图从合成视频恢复伪动作，而是利用其中可靠的几何信息增强感知模块
- **感知-控制解耦训练**: 将VLA的视觉backbone和动作头分开训练，分别使用不同数据源，可能比端到端混合更有效
- **特征漂移问题**: 动作微调会破坏感知表征的质量，需要在微调中保持空间锚点
- **数据效率**: 25条真实示范 + 75条合成视频 + GRA就能达到接近大量真实示范的效果
- **局限**: 目前仅在简单pick-and-place任务上验证，复杂长时域任务的适用性未知；geometric waypoint pipeline依赖多步处理(pose→retarget→sim→project)，累积误差可能影响效果

## 相关论文
- Human-to-robot video generation (Wan 2.2, etc.): 合成视频生成
- Pseudo-action recovery (inverse dynamics, retargeting): 现有合成数据利用方式
- Non-action supervision line: 2D waypoint regression, future-frame prediction, hand-centric video pretraining
- OpenVLA-OFT: 基线VLA架构
