# ArXiv 论文追踪 | 具身智能 / VLA / 世界模型

> 本期日期范围: 2026-06-06 至 2026-06-13
> 筛选标准: 一线研究机构 (Stanford, CMU, Berkeley, Mila, MIT 等) + 有架构图或实验结果 + 近期高热度

---

## 📄 Paper 1: WEAVER

**标题**: WEAVER: Better, Faster, Longer: An Effective World Model for Robotic Manipulation

**arXiv ID**: [2606.13672](https://arxiv.org/abs/2606.13672)

**作者**: Arnav Kumar Jain (Mila/Université de Montréal), Yilin Wu (CMU), Jesse Farebrother (Mila/McGill), Gokul Swamy (CMU), Andrea Bajcsy (CMU)

**机构**: Mila, CMU, McGill

**方向**: Robot World Model / Manipulation

---

### 一句话总结

WEAVER 是一个多视角世界模型，通过 Flow Matching 损失联合预测未来潜空间和奖励值，同时满足**高保真度**、**长时序一致性**、**高效率**三个目标。

---

### 核心问题

世界模型 (World Model) 对机器人有三大应用价值: 策略评估、策略改进、测试时规划。但现有方法无法同时满足三个需求:

1. **保真度 (Fidelity)**: 模拟轨迹需与现实相关
2. **一致性 (Consistency)**: 长期预测需连贯
3. **效率 (Efficiency)**: 推理需快速

例如视频生成模型保真度高但效率低; JEPA 类模型潜空间不可解码; Dreamer-v4 从头学 encoder 泛化差。

---

### 核心方法

**WEAVER = World Estimation Across Views for Embodied Reasoning**

**架构设计**:

1. **多视角预测**: 使用 Ctrl-World 的多视角生成和记忆架构
2. **潜空间预测**: 采用 JEPA 的未来潜空间预测而非图像重建作为主要训练目标
3. **Flow Matching 损失**: 预测 $x_t^1 - x_t^0$ (velocity)，而非重建像素
4. **Diffusion Forcing**: 独立采样噪声层级，提升长时序一致性
5. **预训练 Encoder**: 使用 Stable Diffusion 3 VAE encoder 而非从头学习
6. **奖励预测头**: 不需要外部 VLM 裁判即可评估

**Memory 机制**:
- **Long-term sparse memory** $\mathbf{z}^{mem}_t$: 每隔 $k$ 帧取一帧，捕捉长期上下文
- **Short-term history** $\mathbf{z}^{hist}_t$: 最近两帧，捕捉动作的短期后果

**效率优化**:
- KV Cache 加速 (memory 和 history tokens 跨去噪步复用)
- SPRINT blocks: 激进地 drop patch tokens
- 5-10× 推理加速

---

### 核心公式

**World Model 损失 (Flow Matching)**:

$$
\mathcal{L}^{WM}(\phi) = \mathbb{E}_{x^0_t, x^1_t, \tau} \left[ \left\| (x^1_t - x^0_t) - f_\phi(\mathbf{z}^{hist}_t, \mathbf{z}^{mem}_t, \mathbf{a}_t, x^\tau_t, \tau) \right\|^2_2 \right]
$$

其中 $x^\tau_t = \tau x^1_t + (1-\tau) x^0_t$，$\tau \in [0,1]$

**proprioceptive state prediction**: 显式预测机器人配置（关节位置 + 夹爪宽度），对可变形物体操作至关重要

---

### 实验结果

| 应用场景 | 指标 | 结果 |
|---------|------|------|
| Policy Evaluation | 与现实相关性 ρ | **0.870** |
| Policy Improvement | π₀.5 成功率提升 | **+38%** (无真实交互) |
| Test-time Planning | 成功率提升 | **+14%**，速度 **5-10×** |

**五个真实机械臂任务**: pick-and-place 到可变形物体操作

**OOD 泛化**: 优于 prior world models

**代码/模型**: https://arnavkj1995.github.io/WEAVER/

---

### 对具身智能算法工程师的启发

1. **三个目标联合优化**: WEAVER 的核心洞察是单独优化保真度/一致性/效率都不够，必须同时满足。这对实际部署有重要指导意义。
2. **Flow Matching > Diffusion**: 在这个世界模型任务上，flow matching 比标准 diffusion 在效率和一致性上都有优势。
3. **多视角记忆机制**: 分层记忆 (sparse memory + short-term history) 是处理长程一致性的有效方式。
4. **预训练 encoder 的重要性**: 直接用 SD3 VAE encoder 而非从头训练，大幅提升 OOD 泛化。
5. **无 VLM 裁判的奖励预测**: 训练一个奖励预测头，可以在不调用慢速 VLM 的情况下评估策略质量。

---

## 📄 Paper 2: Mana

**标题**: Mana: Dexterous Manipulation of Articulated Tools

**arXiv ID**: [2606.13677](https://arxiv.org/abs/2606.13677)

**作者**: Zhao-Heng Yin, Guanya Shi, Pieter Abbeel, C. Karen Liu

**机构**: UC Berkeley, CMU, Stanford, Amazon FAR

**方向**: Dexterous Manipulation / Sim-to-Real / Articulated Tools

---

### 一句话总结

Mana 将灵巧操控articulated工具重新定义为动画问题，通过"粗到细"管线自动生成抓取关键帧和操控轨迹，实现零样本sim-to-real迁移。

---

### 核心问题

**Articulated tool 操作为何困难**:

1. **力控复杂性**: 手指必须在摩擦锥内施加精确接触力才能稳定操控工具
2. **耦合动力学**: 多手指和工具形成紧耦合系统，一个关节的微小误差会传播并放大
3. **Teleoperation 局限**: 现有遥操作系统基于位置控制，难以产生精确且足够的力
4. **端到端 RL 探索困难**: 高维手部动作空间 + 长程任务，从头探索极难

---

### 核心方法

**Mana = Manipulation Animator**

**核心洞察**: 将灵巧操作重新定义为**动画问题**。在动画领域，艺术家定义关键帧 (keyframes)，然后系统自动插值生成流畅动画。类似地:

1. **关键帧生成 (Coarse)**: 人类用几个点击指定功能affordance区域，自动生成多样抓取关键帧
2. **轨迹规划 (Fine - MP)**: GPU加速 RRT-Connect 连接无碰撞的到达和预抓取轨迹
3. **强化学习 (Fine - RL)**: 仅用于接触丰富的阶段 (抓取 + 工具操作)，短时 horizon

**Pipeline 概览**:

```
Tool Mesh + Affordance Click → Grasp Keyframes → Motion Planning → RL In-betweening → Manipulation Trajectories
```

**数据生成几乎全自动**: 每个工具 <1 分钟人工标注

**四类工具测试**: tongs (长程协调), pliers (高精度), syringes (高阻抗), clothespins (精密力控)

---

### 关键设计

**Grasp Generator**:
- 使用 Enhanced Lightning Grasp 生成多样接触点
- 优化 fingertip contacts，平衡 manipulation effectiveness 和 grasp stability

**Trajectory Generator**:
- **Pre-grasping**: GPU RRT-Connect 规划到达轨迹
- **Grasping**: 几何策略 + 必要时的短程 RL
- **In-hand actuation**: RL 处理接触丰富的力控，reward 包含 tool pose + hand pose + contact terms

**Force Randomization**: 随机化 PD 增益、物体物理属性、力扰动、动作噪声 → 训练鲁棒策略

**Visuomotor Policy**: Point-cloud-conditioned Transformer Diffusion Policy

---

### 实验结果

在 Allegro hand 上:
- 4 种不同机械挑战的工具全部成功
- 零样本 sim-to-real 迁移
- 所有 grasping 和 finger control 完全自主（除了 tool-to-site 转换用腕部遥操作）

**Force 控制**: 3-7N 精确力控

---

### 对具身智能算法工程师的启发

1. **关键帧思维**: 将长程任务分解为关键帧 + 短程插值，避免端到端 RL 的探索灾难
2. **Sim-to-Real 的数据生成**: 数据生成几乎全自动（<1分钟/工具），这才是 scalable 的路径
3. **力控是核心**: articulated tool 操作的核心挑战是力控，不是位置控制
4. **Point cloud > RGB**: 对于精密操作，点云比 RGB 提供更可靠的 3D 几何信息
5. **Domain Randomization 的重要性**: Force randomization 是 sim-to-real 成功的关键

---

## 📄 Paper 3: Flow Reversal Steering (FRS)

**标题**: Improving Robotic Generalist Policies via Flow Reversal Steering

**arXiv ID**: [2606.13675](https://arxiv.org/abs/2606.13675)

**作者**: Andy Tang, William Chen, Andrew Wagenmaker, Chelsea Finn (Stanford), Sergey Levine (Berkeley)

**机构**: Stanford, UC Berkeley

**方向**: VLA / Generalist Policy / Policy Steering / Flow Matching

---

### 一句话总结

Flow Reversal Steering (FRS) 将次优但"合理"的动作通过 flow policy **反向**传递找到对应噪声，再映射到 generalist 的优质动作模式，实现语义级别的策略引导。

---

### 核心问题

**Generalist policies 的困境**:

- 预训练的大型机器人基础模型 (如 π₀.5) 学到了丰富的行为先验
- 但面对新任务时，直接命令 policy 往往失败
- 标准做法是添加演示数据 + 重新训练，但成本高

**现有 Steering 方法的问题**:
- Diffusion/Flow policy 的噪声空间缺乏直观结构
- 过往工作依赖昂贵的试错式 RL 来找到好的噪声值
- 语义推理器 (VLM) 能做高层推理，但无法直接生成精细的低层动作

---

### 核心方法

**FRS = Flow Reversal Steering**

**核心洞察**: Flow 策略的噪声和动作之间存在**确定性的、可逆的**映射。

**三步流程**:

1. **语义推理**: Human 或 VLM 提供粗略的参考动作 (如"往右移动"或"reach for bowl")
2. **Flow 反向**: 将这个粗糙动作通过 flow 策略**反向**传递 (积分 ODE backward)，找到对应的噪声 $a_0$
3. **正向去噪**: 将噪声通过标准 flow 去噪得到精细的 in-distribution 动作

**Flow 反向公式** (Euler 积分):

$$
a_{t-h} \leftarrow a_t - v_\theta(a_t, t|o) \cdot h, \quad t \in \{1, 1-h, \dots, h\}
$$

这不需要修改任何模型，只需与标准去噪相同的计算量，只是从动作开始而非从噪声开始。

**为什么有效**: 反向传播后的重建动作 $\hat{a}_1$ 与原始参考 $a_1$ 相似但不完全相同，同时是 generalist 的 in-distribution 动作。

---

### 三种应用场景

**1. Zero-shot FRS (直接执行)**:
- 将粗粒度语义指导 ("往右") 转换为精细机器人动作
- 人类或 VLM 只需提供方向性提示

**2. Diffusion Steering via Behavior Cloning (DSBC)**:
- 执行 FRS 得到 (噪声, 动作) 对
- 用行为克隆训练 auxiliary noise policy $\pi_\phi^{noise}$
- 训练 1 分钟内 + 10 条轨迹 → 高达 **95%** 的任务成功率提升

**3. RL 引导 (DSRL + FRS)**:
- 用 FRS 成功案例作为 RL 的先验数据
- 在 base VLA 几乎必定失败的任务上也能有效改进

---

### 核心公式

**Flow 反向**:

$$
\hat{a}_0 \leftarrow \mu_\theta^{-1}(a_1, o)
$$

**DSBC 目标**:

$$
\min_\theta \mathbb{E}_{o, a_0 \sim \pi_\phi^{noise}} \left[ \left\| a_0 - \mu_\theta(o) \right\|^2 \right]
$$

---

### 实验结果

| 实验 | 指标 | 结果 |
|------|------|------|
| LIBERO 仿真 | 零样本任务成功率提升 | **显著** (详见论文) |
| DSBC | 1 分钟训练 + 10 条轨迹 | 最高 **95%** 绝对提升 |
| 真实机器人 | 厨房任务 | 有效 |

**与 π₀.5 结合**: 显著提升通用策略的适应性

---

### 对具身智能算法工程师的启发

1. **噪声空间即先验**: Flow policy 的噪声空间编码了行为的隐空间，找到好的噪声值就能找到好的行为模式
2. **语义到动作的桥梁**: FRS 解决了 VLM "能推理但不能操作" 的问题——让 VLM 做语义推理，让 generalist policy 生成动作
3. **快速适应**: DSBC 证明 1 分钟 + 少量数据就能显著提升新任务成功率，适合实际部署
4. **RL 的语义 bootstrapping**: 用 FRS 的语义知识引导 RL，解决标准 RL 在某些任务上无法改进的问题
5. **无需修改模型**: FRS 不需要训练或微调 generalist policy，只需利用其隐式先验

---

## 📄 Paper 4: GIVE (补充推荐)

**标题**: GIVE: Grounding Human Gestures in Vision-Language-Action Models

**arXiv ID**: [2606.13435](https://arxiv.org/abs/2606.13435)

**作者**: Pengfei Liu, Gen Li, Junqiao Fan 等 (NTU)

**机构**: Nanyang Technological University

**方向**: HRI / Gesture Understanding / VLA

---

### 一句话总结

GIVE 通过双路径 (视觉 + 语义) 将人类手势注入预训练 VLA 模型，在人机交互场景中实现意图理解，无需修改 VLA 架构。

---

### 核心方法

**双路径手势引导**:

1. **视觉路径 (Visual Gesture Prompting)**:
   - 手部关键点估计 → 骨骼 overlay 到机器人视角
   - Pointing gesture → 食指指尖射线，明确指向目标物体
   - 将手势信息投影回 2D 图像平面作为 visual overlay

2. **语义路径 (Semantic Intent Parsing)**:
   - VLM 全局理解场景，生成手势和任务的高层描述
   - 手势稳定 1 秒后才触发 VLM (避免频繁调用)
   - 语义描述是 object-agnostic 的 ("target object" 而非具体名称)

**关键洞察**: 视觉提供精确空间 grounding，语义提供高层意图理解，两者互补。

---

### 实验结果

| 指标 | 提升 |
|------|------|
| 目标物体识别准确率 | **+40%** |
| 整体任务成功率 | **+80%** |

真实机器人: Galaxea R1-Lite 双臂机器人

---

### 对具身智能工程师的启发

1. **手势作为第三模态**: 语言只携带 30-35% 的交互意图，手势等非语言线索对 HRI 至关重要
2. **不修改架构的注入**: 直接通过 visual overlay 和 semantic concatenation 注入新模态，无需重新训练 VLA
3. **双路径互补**: 单一路径不够，视觉 + 语义的组合才能实现 robust intent grounding

---

## 📄 Paper 5: GeoHAT (补充推荐)

**标题**: GeoHAT: Geometry-Adaptive Hybrid Action Transformer for Mobile Manipulation

**arXiv ID**: [2606.13394](https://arxiv.org/abs/2606.13394)

**作者**: Xiangyu Zhu, Renjun Wu, Luzhou Ge, Jinyan Liu, Xuesong Li (BIT)

**机构**: Beijing Institute of Technology

**方向**: Mobile Manipulation / Whole-Body Control / 3D Perception

---

### 一句话总结

GeoHAT 通过轻量级 Fourier 空间编码器 + 可靠性感知门控融合 + 混合全身动作解码器，实现 79.3% 成功率，比最强基线高 23.7%。

---

### 核心设计原则

**核心原则**: "Geometry should be injected only where reliable and attended to only where needed."

**三大机制**:

1. **轻量级 Fourier 空间编码器**: 不使用重 3D encoder，将 PointMap 坐标通过 Fourier MLP 映射为几何 tokens (仅 1.4M 参数)

2. **可靠性感知门控融合**: 
   - 每个 token 有独立 gate $\gamma^i \in [0,1]$
   - Gate 由语义特征、几何特征、深度有效性共同调制
   - 不可靠区域的几何信息被抑制

3. **混合全身动作解码器**:
   - Arm 和 Base 分解为不同子空间
   - Query-level sparse cross-attention: 每个子空间只关注其任务相关的视觉上下文
   - Causal masked self-attention: 时序内 (同一 timestep arm-base 协调) 和时序间 (跨 timestep) 依赖

---

### 实验结果

| Benchmark | 指标 | 结果 |
|-----------|------|------|
| ManiSkill-HAB | 平均成功率 | **79.3%** |
| vs. Strongest baseline | 提升 | **+23.7%** |
| 多步任务 (需 base 移动) | 提升 | **~2×** |

真实世界实验也验证了一致的改进。

---

### 对具身智能工程师的启发

1. **深度可靠性不是 uniform 的**: 在移动操作中，深度传感器在反射表面、物体边界、远处区域都会有噪声，逐 token 的门控比全局处理更有效
2. **Arm 和 Base 需要不同的视觉上下文**: 机械臂需要局部接触几何，底座需要更广的空间上下文，统一处理会限制性能
3. **Fourier 特征映射的威力**: 无需沉重的 3D encoder，轻量级 MLP + Fourier encoding 就能捕捉高频几何结构
4. **稀疏注意力 > 密集注意力**: query-level sparse cross-attention 在保持性能的同时大幅降低计算量

---

## 本期总结

| 论文 | 机构 | 方向 | 核心贡献 |
|------|------|------|----------|
| **WEAVER** | CMU/Mila | World Model | 首个同时满足 fidelity/consistency/efficiency 的 manipulation WM |
| **Mana** | Berkeley/Stanford | Dexterous Manipulation | 动画思维的 sim-to-real 框架，零样本迁移到多种工具 |
| **FRS** | Stanford/Berkeley | VLA Steering | Flow 反向找到噪声空间中的好行为模式 |
| **GIVE** | NTU | HRI / Gesture | 双路径手势意图注入 VLA |
| **GeoHAT** | BIT | Mobile Manipulation | 几何自适应融合 + 混合全身解码 |

**主题趋势**:
1. **World Model 实用性**: WEAVER 展示了 WM 在真实机器人硬件上的评估、改进、规划三大应用
2. **Flow Matching 崛起**: 多篇论文都采用 flow matching 而非标准 diffusion，说明 flow matching 在机器人策略学习中越来越受青睐
3. **精细控制**: 力控、灵巧手、全向移动等精细控制问题持续受到关注
4. **语义 + 低层的桥接**: 如何让高层语义推理连接到低层精细动作是共同主题

---

*Generated: 2026-06-13*
*Total papers in this issue: 5*